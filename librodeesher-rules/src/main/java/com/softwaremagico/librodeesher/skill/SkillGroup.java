package com.softwaremagico.librodeesher.skill;

/**
 * Loose classification of a skill used to tweak minor rules (e.g. firearm suppression skills,
 * "Chi Powers" skills), inferred from its name prefix.
 */
public enum SkillGroup {
    STANDARD,
    FIREARM,
    CHI
}
