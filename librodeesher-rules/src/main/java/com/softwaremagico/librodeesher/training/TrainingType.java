package com.softwaremagico.librodeesher.training;

/**
 * Whether a profession is cheaper, more expensive, or unaffected regarding a training's background
 * point cost, as marked by a "+"/"-" prefix in the legacy "costes" section.
 */
public enum TrainingType {
    STANDARD,
    FAVOURITE,
    FORBIDDEN
}
